# Secrets PostgreSQL. ⚠️ Valeurs de TEST uniquement — ne pas committer en prod.
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: sans-limite
type: Opaque
stringData:
  POSTGRES_USER: sl_user
  POSTGRES_PASSWORD: sl_password_test
  POSTGRES_DB: sans_limite
