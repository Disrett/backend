{
  "name": "sans-limites",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo Sans Limites — frontend Next.js + backend NestJS reliés",
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix backend",
    "dev:front": "npm run dev --prefix frontend",
    "dev:back": "npm run start:dev --prefix backend",
    "build:front": "npm run build --prefix frontend",
    "seed": "npm run db:seed --prefix backend"
  }
}
